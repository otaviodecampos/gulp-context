var utils = require('./utils')
    , GulpContext = require('./gulpcontext')
    , path = require('path')
    , debug = require('./debug');

module.exports = function defaultParser(vars) {

    return vars;
}